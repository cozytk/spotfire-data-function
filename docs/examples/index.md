---
title: 예제 모음
nav_order: 4
has_children: true
---

# 예제 모음
{: .no_toc }

모든 예제는 **실제로 실행해 검증한 코드**입니다.
`data/` 폴더의 CSV를 Spotfire에 불러온 뒤 스크립트를 그대로 붙여 넣으면 동작합니다.

각 예제는 같은 형식으로 되어 있습니다.

1. **시나리오** — 어떤 업무 상황인가
2. **왜 Spotfire 기본 기능으로는 어려운가** — 데이터 함수를 쓸 이유
3. **데이터 함수 설정** — 입력/출력 파라미터를 어떻게 잡을 것인가
4. **스크립트** — 붙여 넣어 바로 쓰는 코드
5. **핵심 포인트** — 코드에서 반드시 이해하고 넘어갈 것
6. **이렇게 물어보세요** — 같은 결과를 AI에게 얻어내는 프롬프트
7. **확인 & 응용** — 검증 방법과 다음 과제


## Level 1 · 정제 기초

| # | 예제 | 난이도 | 무엇을 배우나 |
|---|---|---|---|
| 01 | [중복 행 제거와 "키 기준 최신값만 남기기"](./01-dedup-latest/) | ★☆☆ | Spotfire에 아예 없는 기능 1순위. 완전 중복 제거에서 시작해, 같은 키의 재측정 이력 중 최신 1건만 남기는 데까지 확장합니다. |
| 02 | [결측치·0값 행 정리 (안전하게)](./02-missing-and-zero/) | ★☆☆ | dropna 3종 세트. "0이 있는 행 지우기"를 전체 컬럼에 적용하면 왜 위험한지, 어떻게 고쳐야 하는지까지. |
| 03 | [값 치환과 조건부 파생 컬럼 (그리고 inplace 함정)](./03-replace-and-derive/) | ★☆☆ | replace / fillna / np.select 로 코드값 정리와 다중 조건 등급 부여를 한 번에. 최신 pandas에서 inplace가 조용히 실패하는 이유도 짚습니다. |
| 04 | [두 테이블 조인 후 조건별로 여러 테이블로 분리 출력](./04-join-and-split/) | ★★☆ | merge + indicator 로 조인 실패까지 진단하고, 결과를 공정별 3개 테이블로 쪼개 한 번에 내보냅니다. 기존 실습 스크립트의 개선판. |

## Level 2 · 재구조화

| # | 예제 | 난이도 | 무엇을 배우나 |
|---|---|---|---|
| 05 | [Wide → Long 변환 (센서 200개 컬럼을 3개로)](./05-wide-to-long/) | ★★☆ | melt 한 줄로 컬럼이 곧 데이터인 형태를 정규화합니다. 컬럼이 수십~수백 개일 때 Spotfire 기본 기능으로는 사실상 불가능한 작업. |
| 06 | [피벗 / 크로스탭 — 설비 × 챔버 불량률 매트릭스](./06-pivot-crosstab/) | ★★☆ | pivot_table 로 집계 매트릭스를 만들고, 합계행·합계열까지 붙여 새 데이터 테이블로 내보냅니다. |
| 07 | [그룹 통계를 원본 행에 되붙이기 (transform)](./07-group-stats-transform/) | ★★☆ | 설비·공정별 평균/표준편차/중앙값을 계산해 원본 행 수를 유지한 채 Z-score, 순위, 백분위를 한 번에 만듭니다. |
| 08 | [결측치를 "지우지 않고" 똑똑하게 채우기](./08-impute-smart/) | ★★☆ | 그룹 중앙값 대치, 시간 기반 선형 보간, 앞/뒤 값 채움을 컬럼 성격에 맞게 골라 적용하고, 무엇을 채웠는지 흔적을 남깁니다. |

## Level 3 · 시계열과 공정 분석

| # | 예제 | 난이도 | 무엇을 배우나 |
|---|---|---|---|
| 09 | [시간 단위 재집계와 "비어 있는 시간" 채우기](./09-timeseries-resample/) | ★★☆ | 불규칙한 이벤트 로그를 시간당/일별 격자로 재집계하고, 데이터가 아예 없는 구간을 0 또는 결측으로 명시적으로 만들어 냅니다. |
| 10 | [이동평균·이동표준편차·EWMA로 트렌드 잡기](./10-rolling-trend/) | ★★☆ | 설비별로 최근 N장 이동 통계를 계산해 노이즈를 걷어내고, 단기 평균이 장기 평균을 이탈하는 시점을 잡아냅니다. |
| 11 | [이상치 탐지 4종 비교 (IQR / Z / MAD / Hampel)](./11-outlier-detect/) | ★★★ | 같은 데이터에 네 가지 이상치 기준을 동시에 적용해 결과를 비교하고, 몇 개 기준에서 걸렸는지로 신뢰도를 매깁니다. |
| 12 | [SPC 관리도 + 공정능력지수(Cp/Cpk) + Western Electric 룰](./12-spc-control-chart/) | ★★★ | 관리한계선을 직접 계산하고, 8개 룰 중 4개를 구현해 "관리 이탈" 시점을 자동으로 찾아냅니다. 데이터 함수의 진가가 가장 잘 드러나는 예제. |
| 13 | [설비별 드리프트 기울기와 규격 도달 예상 시점](./13-drift-slope/) | ★★★ | 그룹마다 회귀선을 적합해 기울기·R²를 데이터로 뽑고, 현재 추세라면 언제 규격을 벗어나는지 예측합니다. |
| 14 | [시간 근접 조인 (merge_asof) — 계측 시점의 설비 상태 붙이기](./14-merge-asof/) | ★★★ | 키가 정확히 일치하지 않는 두 시계열을 "가장 가까운 직전 값" 기준으로 조인합니다. Spotfire 기본 기능으로는 불가능에 가까운 작업. |
| 15 | [연속 이벤트를 하나의 "세션"으로 묶기](./15-event-sessionize/) | ★★★ | 30분 이상 끊기면 새 세션. 흩어진 알람 로그를 사건 단위로 묶어 지속시간·건수·대표 원인을 만들어 냅니다. |
| 16 | [정규식으로 로그 메시지에서 구조화된 컬럼 뽑아내기](./16-regex-parse-log/) | ★★★ | 한 덩어리 텍스트에 갇혀 있는 알람 코드·LOT·수치를 컬럼으로 꺼내 분석 가능한 데이터로 바꿉니다. |

## Level 4 · 통계·자동화·Spotfire 고유 기능

| # | 예제 | 난이도 | 무엇을 배우나 |
|---|---|---|---|
| 17 | [상관 매트릭스를 "쌍(pair) 테이블"로 뒤집기](./17-correlation-pairs/) | ★★☆ | 20×20 상관 매트릭스에서 의미 있는 상관쌍만 뽑아 랭킹 테이블로 만듭니다. 센서가 많을수록 강력해집니다. |
| 18 | [설비 간 차이가 진짜인가 — ANOVA와 사후 다중비교](./18-group-test-anova/) | ★★★ | 눈으로 보면 달라 보이는 그룹 평균이 통계적으로 유의한지 검정하고, 어느 쌍이 다른지까지 짚어 냅니다. |
| 19 | [파레토 분석과 ABC 등급 분류](./19-pareto-abc/) | ★★☆ | 누적 기여율을 계산해 "상위 몇 개가 전체의 80%를 차지하는가"에 답하고, A/B/C 등급을 자동으로 매깁니다. |
| 20 | [문서 속성으로 파라미터화 — 한 번 만들어 계속 쓰는 데이터 함수](./20-parameterize/) | ★★★ | 그룹 컬럼·집계 방식·기간·임계값을 전부 Value 입력으로 받아, 코드를 고치지 않고 화면에서 조작하는 범용 요약 함수를 만듭니다. |
| 21 | [마킹(Marking) 입력 활용 — 선택한 구간 vs 나머지 비교](./21-marking-compare/) | ★★★ | Spotfire에서만 가능한 입력 방식. 사용자가 차트에서 드래그로 고른 행을 그대로 함수의 입력으로 받아 "이 구간이 뭐가 다른가"를 즉시 계산합니다. |
| 22 | [LOT 프로파일 클러스터링 (KMeans)](./22-kmeans-cluster/) | ★★★ | 여러 공정의 계측값을 하나의 프로파일로 묶어 비슷한 LOT끼리 자동 분류하고, 각 군집의 특징을 해석 가능한 표로 만듭니다. |
| 23 | [차트 이미지를 데이터 함수로 만들어 대시보드에 띄우기](./23-matplotlib-image/) | ★★★ | Spotfire 시각화로는 그릴 수 없는 그림(설비별 분포 비교, 관리도 격자)을 matplotlib으로 그려 문서 속성 이미지로 내보냅니다. |
| 24 | [공개 데이터셋 종합 실습 (titanic / iris / diabetes)](./24-public-datasets/) | ★★☆ | 사내 데이터 없이도 연습할 수 있도록, 잘 알려진 3개 데이터셋으로 전처리·파생·요약의 전 과정을 한 번에 훑습니다. |


---

## 난이도 표기

| 표기 | 의미 |
|---|---|
| ★☆☆ | pandas를 처음 봐도 따라갈 수 있음 |
| ★★☆ | 함수 하나의 동작을 이해해야 함 |
| ★★★ | 여러 개념이 조합됨 — 프롬프트로 얻고 검증하는 연습에 적합 |
